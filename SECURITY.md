# 安全政策（Security Policy）

## 支持版本

| 版本 | 是否维护 |
|------|----------|
| v6.3.x（最新） | ✅ |
| 更早版本 | ❌ 建议升级到最新 |

## 报告漏洞

如发现安全漏洞，**请勿公开 Issue**，以免被利用。请通过以下方式私密报告：

- GitHub Security Advisory（推荐）：在本仓库 `Security → Report a vulnerability` 提交
- 或邮件联系维护者（见仓库主页联系方式）

报告时请尽量提供：
1. 漏洞类型与影响面
2. 复现步骤 / PoC
3. 建议修复方向（如有）

我们会在 **5 个工作日内**确认收到，并在评估后尽快修复与致谢。

## 本项目的隐私与安全模型

分身是**本地优先（local-first）**软件：

- 项目、角色、消息、元神资料等数据默认存储于**用户本机** SQLite（`~/.fenshen/` 或仓库 `data/`），不上传任何云端服务器。
- 仅在与大模型对话时，文本会发往所配置的 LLM 服务（默认 DeepSeek）。请勿在对话中粘贴密钥、口令等敏感信息。
- 局域网（LAN）模式会开放 `8002` 端口供同网段设备访问，已内置令牌护栏；**切勿在公网无防护地暴露该端口**。

## 密钥管理约定（贡献者必读）

- **任何密钥、口令、AccessKey、AppSecret 都不得提交进仓库。**
- 仓库已通过 `git filter-repo` 清理历史明文密钥，并配置 `.gitignore` 忽略 `*.key` / `*.pfx` / `*.p12` / `.env` / `secrets/`。
- 如需注入凭据，请使用环境变量或本地未跟踪文件（已被 gitignore 覆盖）。
