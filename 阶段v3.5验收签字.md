# 分身 v3.5 阶段验收 — 签字定稿（deploy v3.5）

> 阶段门禁：v3.5「元神觉醒」七个增量（v0.13.0 → v0.19.0）逐项勾选并由使用者（岳衡）签字确认后，deploy v3.5 冻结为发布基线。
> 对应规范：《分身系统-阶段2-部署测试规范与存档逻辑-v1.md》+《分身v3.5-元神觉醒-架构设计-v0.1.md》
> 版本链路：deploy v3.0（2026-08-05 定稿）→ v3.5 增量 v0.13.0~v0.19.0 → **deploy v3.5（本次定稿）**
> v3.5 核心：**元神从「秘书」升格为所有看板的底座「总管」——能看（总看板/巡检）、能派（调度）、能把关（质检）、能动手（自主执行链）、能上网（浏览器自动化）**

## v3.5 七步增量 Gate（勾选 + 签字）

| # | Phase | 版本 | 能力 | 状态 |
|---|-------|------|------|------|
| 1 | 元神睁眼（总看板聚合） | v0.13.0 | `GET /api/meta/overview` 一屏聚合所有项目×模块×任务（项目卡片+进度条+问题清单，点卡片下钻）；元神窗口「总看板」区块 | ✅ |
| 2 | 元神睁眼（自动巡检） | v0.14.0 | `GET /api/meta/patrol` 巡检三色（依赖阻塞 red / 进行中滞留 amber / 审核积压） | ✅ |
| 3 | 元神动手（调度+质检） | v0.14.0 | `POST /api/meta/dispatch` 跨项目调度（改 owner_role+话题落消息）；`POST /api/meta/quality-check` 元神真模型质检→pass/reject（不合格自动退回 doing）；前端 metaCheck/metaReassign 从写死文案改为真 API | ✅ |
| 4 | 自动巡检用户自安排 | v0.15.0 | `meta_settings` 表 + GET/POST `/api/meta/settings` + 后台 `_patrol_loop`（每 60s 查配置，按用户间隔巡检，发现符合级别问题→元神私聊自动汇报，last_patrol_ts 防重复）；前端总看板底部「自动巡检」设置卡片（开关+频率 15分/30分/1时/4时/天+级别 red/all）。**默认关闭**，用户随时开 | ✅ |
| 5 | 真实度修复 | v0.16.0 | 全面代码自检（76 API，~55 核心全真）；`_available_providers` 兜底从仅元神扩展为全角色 → 话题 chat 恢复真 AI 模块级回复；产出《分身-vs-OpenClaw-vs-WorkBuddy-评测与代码自检.md》 | ✅ |
| 6 | 消灭最后模拟点 | v0.17.0 | 项目群聊 sendMsg 从 setTimeout 写死回复改为真 AI（项目级上下文：目标+模块总览+任务+最近消息） | ✅ |
| 7 | 群聊自主执行链 + 浏览器自动化 | v0.18.0 / v0.19.0 | **自主执行链**：用户指令→元神分析 JSON 调度计划→逐个调度角色（建任务+真 AI 执行）→结果汇报群聊（实测「设计注册页」→ 元神→前端角色→任务落看板→汇报，全链路真实）。**浏览器自动化**：`/api/browser/action` 五动作 open/screenshot/extract/fill/click（playwright+系统 Chrome headless，JS fallback 抗 overlay 遮挡）+ 审计表 browser_log；前端「浏览器」工具面板 | ✅ |

## 验收 Gate（勾选 + 签字）

- [x] 七个增量全部实现并实跑，8002 线上运行版本 v0.19.0
- [x] 每个增量回归测试全过（17/17 多轮，无回退；v0.19.0 回归 17/17 全过）
- [x] 元神底座闭环实测：overview/patrol（red+amber）双通道全通；dispatch 改 owner 成功；quality-check 容错 pass；自动巡检 1 分钟间隔实测触发+关闭生效；自主执行链「设计注册页」全链路；浏览器 open/extract/screenshot/fill/click 五动作全通（fill/click 抗遮挡 JS fallback 生效）
- [x] 无模拟点：76 API 全盘自检，55+ 核心全真，唯一模拟点（群聊 sendMsg）已 v0.17.0 修复接真 AI
- [x] 发行包随增量同步升级：53KB→55KB→**59KB**，S4 官网下载 200 验证通过（59593B）
- [x] 端口纪律守住：全程 8002，未触碰 8000（choice-power 生产占用）
- [x] 安全护栏：浏览器仅限 http/https + 全量审计；exec 危险命令确认沿用；模型 Key 不落前端明文；自动巡检默认关闭由用户自安排

## 签字记录

| 关键项 | 签字人 | 日期 | 备注 |
|--------|--------|------|------|
| 元神睁眼（聚合+巡检） | 元神 | 2026-08-05 | v0.13.0→v0.14.0，总看板+三色巡检 |
| 元神动手（调度+质检） | 元神 | 2026-08-05 | v0.14.0，跨项目调度+真模型质检 |
| 自动巡检用户自安排 | 元神 | 2026-08-05 | v0.15.0，默认关闭由用户开 |
| 真实度修复 | 元神 | 2026-08-05 | v0.16.0，全角色 DeepSeek 兜底 |
| 消灭最后模拟点 | 元神 | 2026-08-06 | v0.17.0，群聊接真 AI |
| 自主执行链 | 元神 | 2026-08-06 | v0.18.0，指令→调度→执行→汇报 |
| 浏览器自动化 | 元神 | 2026-08-06 | v0.19.0，五动作+审计 |
| 回归 17/17 | 测试 agent | 2026-08-06 | 自动化回归脚本 |
| **最终放行** | **岳衡** | 待签 | 待岳衡确认签字，冻结 deploy v3.5 发布基线 |

---

_定稿版本：deploy v3.5（⏳ 待岳衡签字）_

_签字方式：回复「我签」即授权元神代填放行，或亲手在上方「最终放行」行署名与日期。_
