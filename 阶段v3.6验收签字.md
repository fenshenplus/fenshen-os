# 分身 v3.6 阶段验收 — 签字定稿（deploy v3.6）

> 阶段门禁：v3.6「工具化与模板化」四个增量（v0.20.0 → v0.23.0）逐项勾选并由使用者（岳衡）签字确认后，deploy v3.6 冻结为发布基线。
> 对应规范：《分身系统-阶段2-部署测试规范与存档逻辑-v1.md》
> 版本链路：deploy v3.5（2026-08-06 定稿）→ v3.6 增量 v0.20.0~v0.23.0 → **deploy v3.6（本次定稿）**
> v3.6 核心：**元神/群聊全面「能动真家伙」——对话直接驱动电脑执行与浏览器；项目启动沉淀为可复用模板（模块+目标+角色集）**

## v3.6 四步增量 Gate（勾选 + 签字）

| # | Phase | 版本 | 能力 | 状态 |
|---|-------|------|------|------|
| 1 | 元神工具调用（Function Calling） | v0.20.0 | `META_TOOLS` 两工具 exec_command（shell）+ browser_action（浏览器五动作）；openai 格式 tools + tool_choice=auto；`_meta_chat_with_tools` 多步循环（最多 6 轮，支持连续多工具操作）；工具执行全量落 exec_log/browser_log 审计；危险命令在对话内直接拦截；claude/ollama 通道自动退化 | ✅ |
| 2 | 多项目模板沉淀 | v0.21.0 | `project_templates` 表 + 4 内置模板（标准 Web 应用 / 电商小程序 / 内容社区 / AI 工具应用）+ CRUD（自定义可存可删、内置受保护）+ 首启动幂等 seed；wizard 第 3 步模板选择器（★ 内置标记 + 描述展示 + 一键套用 + 存为模板） | ✅ |
| 3 | 群聊工具调用 | v0.22.0 | `_chat_with_tools` 通用化（agent_id 参数化）；项目群聊 Step 1 元神分析可先调工具查状态再输出调度计划；Step 2 角色执行也可调工具（跑命令验证/查资料）；审计按调用方 agent_id 落库 | ✅ |
| 4 | 模板进阶（目标+角色集） | v0.23.0 | 模板加 `goal`（目标模板）+ `roles`（角色集）；旧库迁移（ALTER 补列）+ 内置模板 upsert 补字段；前端套用模板自动填目标（未填时）+ 自动勾选角色集；存为模板时一并保存目标与角色 | ✅ |

## 验收 Gate（勾选 + 签字）

- [x] 四个增量全部实现并实跑，8002 线上运行版本 v0.23.0
- [x] 每个增量回归测试全过（17/17 多轮，无回退）
- [x] 真实调用实测：
  - 元神对话「看看我桌面有什么文件夹」→ 真执行 `ls ~/Desktop`，汇报真实清单 ✅
  - 元神对话「抓百度热搜榜」→ 连续多步浏览器调用，返回 top.baidu.com 前 30 条真实热搜 ✅
  - 群聊「查一下当前系统时间和日期」→ 真执行 `date` 返回系统时间 ✅
  - 群聊「打开 example.org 看标题」→ 浏览器返回 "Example Domain" + 正文 ✅
  - 模板：4 内置 seed、自定义保存/删除、内置删除拦截、goal/roles 补全（upsert）、套用联动 ✅
- [x] 无模拟点：工具执行全部真实 + 审计落库（exec_log/browser_log）
- [x] 发行包随增量同步升级：59KB→62KB→63KB→**64KB**，S4 官网下载 200 验证通过（64469B）
- [x] 端口纪律守住：全程 8002，未触碰 8000（choice-power 生产占用）
- [x] 安全护栏：工具仅 http/https + 危险命令拦截（对话内不可交互确认，需终端面板手动放行）+ 全量审计；模型 Key 不落前端明文

## 签字记录

| 关键项 | 签字人 | 日期 | 备注 |
|--------|--------|------|------|
| 元神工具调用 | 元神 | 2026-08-06 | v0.20.0，Function Calling 多步循环 |
| 多项目模板沉淀 | 元神 | 2026-08-06 | v0.21.0，4 内置模板+CRUD |
| 群聊工具调用 | 元神 | 2026-08-06 | v0.22.0，元神分析+角色执行均可调工具 |
| 模板进阶（目标+角色集） | 元神 | 2026-08-06 | v0.23.0，goal/roles+套用联动 |
| 回归 17/17 | 测试 agent | 2026-08-06 | 自动化回归脚本 |
| **最终放行** | **岳衡** | 2026-08-06 | ✅ 已签字，冻结 deploy v3.6 发布基线 |

---

_定稿版本：deploy v3.6（✅ 岳衡 2026-08-06 认可签字，已定稿冻结为发布基线）_

_签字方式：回复「我签」即授权元神代填放行，或亲手在上方「最终放行」行署名与日期。_
