# 数据与密钥（本地私有，不提交）
data/
*.key
*.pem

# Python
__pycache__/
*.pyc
.venv/
venv/

# 临时与日志
*.log
.temp/
.DS_Store

# 发行包中间产物（旧构建，不入库）
dist-stage/

# 部署脚本含明文服务器密码，禁止入库
deploy-s4.exp

# 官网安装包（大文件，不在仓库维护）
site/*.zip
site/*.dmg

# PyInstaller 构建产物（不入库）
dist/
build/
*.spec
分身.app/
