# 分身 · 六能力基线档案（v1）

> 建立日期：2026-09-05　基线版本：**0.64.60**
> 用途：**六能力 = 分身产品状态的唯一评判口径**。后续所有开发规划、每轮检测、每次发布，一律按这六项能力的等级变化来评判，不再以「做了几个功能」论进展。
> 配套文档：`分身-产品评估检测标准-v2.md`（检测项与阈值细则）

---

## 1. 六能力是什么

| 编号 | 能力 | 一句话定位 | 为什么算一种「能力」 |
|---|---|---|---|
| **D0** | **大模型能力** | 连得上、记得住、用得省、坏不了 | 信任底线。Key 记不住 / 静默失败 = 产品不可用 |
| **D1** | **蒸馏能力** | 把「本机用户本人」蒸馏成可复用的数字分身 | 差异化核心，决定「像不像你」 |
| **D2** | **元神能力** | 理解意图、调度 agent、交付价值、自主推进 | 决定「能不能替你干活」 |
| **D3** | **Skill 模组能力** | 精选、纯 stdlib、可组合、启动即用 | 决定「干活的手段够不够、干不干净」 |
| **D4** | **工程交付能力** | 一键构建、三端产物、质量门禁、可回滚 | 决定「能不能稳定地交到用户手上」 |
| **D5** | **生产稳定性与可靠性** | 常驻守护、原生模块、数据不丢、自愈 | 决定「能不能长期放心用」 |

**等级量表**（L0 最差 → L4 最优，沿用评估标准 v2 通用量表）：

- **L0** 不可用心（或命中宪法红线，整体不通过）
- **L1** 有雏形，主要靠人工/碰运气
- **L2** 可用，主要路径跑通，有基本检测
- **L3** 可靠，自动化 + 可观测 + 有护栏，异常不静默
- **L4** 卓越，分层/择优/成本可视化 + 自优化

---

## 2. 基线快照（2026-09-05 · v0.64.60）

| 能力 | 当前等级 | 一句话现状 | 本基线周期内的关键进展 |
|---|---|---|---|
| **D0 大模型能力** | **L3** ✅ | Key 跨重装/重启/更新都不丢；上下文滑窗、Token 预算、连接状态面板、主备路由四项已实装 | 0.64.58 双向同步根因闭环 → 0.64.59 phantom 假模型名归一 + 测试连接按钮 → 0.64.60 补齐上下文/Token/连接/路由 |
| **D1 蒸馏能力** | **L2**（基线占位，⚠️ 管线未实装） | 蒸馏管线尚未落地，等级为基线占位而非实测达标；实装后须重测 | — |
| **D2 元神能力** | **L2** | Goal-Mode + 原生质量门已落地（Ralph loop：worker 干活、judge 验收） | 0.64.56 `goal_mode.py` + `native/verify.py` + 7 个 API |
| **D3 Skill 模组能力** | **L2** | 19 个内置 skill 精选；`native/` 纯 stdlib（auth 为零 SDK 范例） | 全程维持「少而精、不堆量」 |
| **D4 工程交付能力** | **L2** | smoke 76/0 门禁；PyInstaller onefile → macOS/Windows 产物；tag 触发 CI | 0.64.54~0.64.60 连续 7 个版本构建发布无阻断 |
| **D5 生产稳定性与可靠性** | **L2** | launchd 守护 KeepAlive 稳定；`native/` 覆盖部分确定性流程；增量迁移不丢数据 | 0.64.58 启动双向同步自愈实测通过 |

**发布门禁达成情况**：D0≥L3 ✅　D5≥L3 ❌（当前 L2）　D1~D4≥L2 ✅　宪法红线 0 命中 ✅
→ **当前门禁结论：D5 未达 L3，属「有条件放行」**（详见 §6）

---

## 3. 逐能力基线卡

### D0 · 大模型能力 —— **L3**

| 检测项 | 实测结果 | 达标 |
|---|---|---|
| API Key 持久化（重装/重启/更新） | 0.64.58 起 `sync_model_configs()` 双向同步（DB ↔ Application Support 互为备份），三项均实测通过；`test_model_key_persistence.py` 5/0 | **L3** ✅ |
| 连接可用性 | 0.64.59 `normalize_ds_model()` 归一 phantom 假模型名（填错也能连通）；显式「🧪 测试连接大模型」按钮；`test_model_name_normalization.py` 8/0 | **L3** ✅ |
| 上下文管理 | 0.64.60 `_context_window()`：系统锚点 + 首条需求锚点恒保，超出 9000 token 预算截断中间，零额外调用 | **L2→L3** ✅ |
| Token 管理 | 0.64.60 `_check_budget()`：每日预算，超额**拦截并明确提示**（不静默放行）；`model_budgets` 表 + 用量统计 | **L2→L3** ✅ |
| 多模型/备用路由 | 0.64.60 `_PROVIDER_HEALTH` 健康态 + `/route` 候选链透明化（不含明文 Key） | **L3** ✅ |
| 配置回显安全 | 前端永不回显明文 Key，留空提交 = 保留现有 Key | 红线 0 命中 ✅ |

- **已知缺口**：Token 仅统计到「今日用量 + 预算拦截」，尚无成本看板与自动降级（L4 项）。
- **提升路线 L3 → L4**：成本看板（按模型/按日聚合）→ 预算达阈值自动降级到备用模型并告知用户。

---

### D1 · 蒸馏能力 —— **L2（基线占位，管线未实装）**

> ⚠️ **诚实标注**：蒸馏管线尚未实装，本等级是「基线占位」，**不是实测达标**。实装后必须按下列检测项重测，届时等级可能下调。

| 检测项 | 现状 | 阈值 |
|---|---|---|
| 身份保真度 | 未测（无管线） | L2≥盲评≤60%；L3≥≤52% |
| 领域知识召回 | 未测 | L2≥70%；L3≥85% |
| 风格一致性 | 未测 | L2≥余弦 0.6；L3≥0.75 |
| 记忆连续性 | 未测 | L2≥80%；L3≥95% |
| 蒸馏安全（授权） | 无违规（无管线即无越权） | 红线 0 违规 |
| 增量蒸馏 | 未测 | L3≥95% 无灾难性遗忘 |
| 成本/时效 | 未测 | L2≤30min；L3≤10min |

- **提升路线 L2 → L3**：先落蒸馏管线 MVP（数据摄入 → 风格/知识抽取 → 可回灌 prompt）→ 建盲评题库（≥50 题）→ 建宪法对抗测试集。
- **前提红线**：蒸馏他人必须显式授权，否则 D1 直接判 L0（与保真度无关）。

---

### D2 · 元神能力 —— **L2**

| 检测项 | 现状 | 阈值 |
|---|---|---|
| 意图理解 | 未系统评测 | L2≥75%；L3≥90% |
| 调度正确性 | `_run_dispatch_job` 派发 owner_role 已可用，未量化 | L2≥85%；L3≥97% |
| 交付价值 | 端到端产出可用，未量化 | L2≥70%；L3≥90% |
| 主动推进 | 0.64.56 Goal-Mode（Ralph loop）已落地，成功率未量化 | L2≥60%；L3≥85% |
| 宪法合规 | 无命中记录，但**无常态化对抗测试集** | 红线必须 0 命中 |
| 上下文连续性 | 0.64.60 上下文滑窗已保障锚点 | L2≥80%；L3≥95% |
| 轻量验收 | `native/verify.py` 原生 gate 已建，占比未达 70% | L3≥70% 走原生 |

- **已知缺口**：缺乏可重复的量化评测集（意图/调度/交付三项均无基线数据），「未量化」是 D2 停在 L2 的主因。
- **提升路线 L2 → L3**：建 30 条含歧义/嵌套意图任务集 → 统计调度准确率 → 把可判定验收的原生 gate 比例提到 70%+ → 宪法对抗测试集常态化。

---

### D3 · Skill 模组能力 —— **L2**

| 检测项 | 现状 | 阈值 |
|---|---|---|
| 注册完整性 | `NATIVE_CAPABILITIES` 与实现一致，无悬空 | L2=100% 无悬空 |
| 冷启动加载 | 19 个内置 skill 启动可用 | L2=100%；L3≤3s |
| 调用成功率 | 未逐 skill 量化 | L2≥95%；L3≥99% |
| 纯净度 | `native/` 纯 stdlib（auth 零 SDK 已验证） | L3=100% 纯 stdlib |
| 少而精 | 19 个，坚持不堆量 | L3=精选≤30 且覆盖≥90% 高频场景 |
| 可组合 | 支持，无编排示例沉淀 | L2 支持；L3 有编排示例 |
| 安全边界 | 0 命中 | 红线 0 命中 |

- **已知缺口**：调用成功率未逐 skill 量化；缺多 skill 串联的编排示例。
- **提升路线 L2 → L3**：给每个 skill 补 happy path 自测 → 沉淀 2~3 个组合编排示例 → 冷启动耗时纳入检测。

---

### D4 · 工程交付能力 —— **L2**

| 检测项 | 现状 | 阈值 |
|---|---|---|
| 构建成功率 | PyInstaller onefile 稳定（含 `/tmp/pyicache` 绕沙箱方案） | L2=100% 可复现 |
| 跨平台产物 | macOS DMG ✅ / Windows exe ✅ / Android APK 在库；**均未签名** | L3=三端齐备且签名 |
| 质量门禁 | `tests/smoke_v40.py` 76/0（fail-closed） | L2=0 失败 |
| 版本单一真源 | `version.py` / Info.plist / 落地页一致 | L2=100% 一致 |
| CI/CD | tag 触发 Windows CI 自动构建发布 | L3=全自动、可回滚 |
| 回滚时效 | 有历史产物可回退，未量化 | L3≤15min |
| 发布可见性 | ⚠️ **下载站已 0.64.60，官网 fenshen.plus 仍 0.64.43** | L2=100% 一致 ❌ |

- **已知缺口（明确阻塞 L3）**：① 三端签名（Apple Dev ID / Windows EV 等待中）；② **官网与下载站版本不一致**；③ 回滚时效未量化。
- **提升路线 L2 → L3**：同步官网版本 → 建立回滚演练计时 → 签名到位后补齐三端签名产物。

---

### D5 · 生产稳定性与可靠性 —— **L2**（门禁未达 L3）

| 检测项 | 现状 | 阈值 |
|---|---|---|
| 常驻守护 | launchd `KeepAlive` 实测自拉正常 | L2=崩溃自拉 ✅；L3=7×24 uptime≥99.5% ❌ |
| 基础能力模块化 | `native/` 覆盖注册/找回密码/看板/导入/产出的**一部分** | L3=100% 确定性通过 ❌ |
| 账号闭环 | 端到端走通（含短信验证码自签） | L2=100%；L3=异常全兜底 |
| 看板真源 | `tasks.stage/track` 唯一真源 | L3=0 漂移 |
| 原生质量门 | `native/verify.py` 已建（file_exists/test_passed/page_reachable/build_ok） | L3=可判定项 100% 走原生 |
| 数据迁移 | `init_db` 增量迁移，升级不丢 | L2=0 丢失 ✅；L3=自动自愈 |
| 配置持久化 | 与 D0 同源，双向冗余 + 启动自愈实测 | **L3** ✅ |
| 服务器管理 | 公网中继隧道可用，每用户独立令牌 | L3=断线自动重连 |
| 容错自愈 | 0.64.58 双向同步实测自动修复 | **L3** ✅ |
| 并发/资源 | 未做 72h 长跑 | L3=72h 无泄漏 ❌ |

- **未达 L3 的三项硬缺口**：① 7×24 uptime 数据未采集；② `native/` 模块化未覆盖全部高频流程；③ 72h 内存/句柄泄漏未测。
- **提升路线 L2 → L3**（对应评估标准 §8 #6）：uptime 埋点统计 → `native/` 吸纳剩余高频确定性流程 → 72h 长跑观测。

---

## 4. 能力看板（版本跟踪表）

> **规则**：每个版本发布必须回填一行。等级只填实测值，**未测的写「未测」不写达标**。

| 版本 | 日期 | D0 | D1 | D2 | D3 | D4 | D5 | 本版本能力提升点 | 门禁 |
|---|---|---|---|---|---|---|---|---|---|
| 0.64.56 | 09-04 | L2 | L2* | L2 | L2 | L2 | L2 | D2 Goal-Mode + 原生质量门 | 有条件 |
| 0.64.57 | 09-04 | L2 | L2* | L2 | L2 | L2 | L2 | D4 产出栏 UI | 有条件 |
| 0.64.58 | 09-04 | **L3** | L2* | L2 | L2 | L2 | L2 | D0 持久化根因闭环 | 有条件 |
| 0.64.59 | 09-04 | **L3** | L2* | L2 | L2 | L2 | L2 | D0 连接可用性（phantom 归一） | 有条件 |
| 0.64.60 | 09-04 | **L3** | L2* | L2 | L2 | L2 | L2 | D0 上下文/Token/连接/路由四项 | 有条件 |

`L2*` = 基线占位（管线未实装，非实测达标）。

---

## 5. 评判规则（后续一律照此执行）

### 5.1 开发规划怎么排
1. **按门禁缺口排序**：先看有没有能力低于门禁线（当前是 **D5 < L3**），缺口项优先级最高。
2. **按宪法红线排序**：任何命中红线的问题，无条件插队到最前。
3. **按「未量化」排序**：标注「未测/未量化」的检测项，优先于「已达标但要锦上添花」的项 —— 没有数据就无法评判，这是 D2 长期停 L2 的根因。
4. 不做与六能力无关的功能堆砌。

### 5.2 每轮检测怎么评
1. **D0 先测**（信任底线，不过则阻断全量）。
2. **D5 再测**（地基）。
3. **D4 门禁**（smoke + 构建）。
4. **D1/D2/D3 抽样评测**（有评测集的用集，没集的先建集再评）。
5. 出结论时**只报六能力等级变化**，不报功能数量。

### 5.3 发布门禁
```
放行条件 = D0 ≥ L3  且  D5 ≥ L3  且  D1~D4 ≥ L2  且  宪法红线 0 命中
```
不满足时的处置：
- **D0 < L3** → **阻断全量**（信任底线不可妥协）。
- **D5 < L3 但 ≥ L2** → 有条件放行，须在评估卡写明遗留风险与补齐时点。
- **任一能力命中红线** → 整体不通过，与等级无关。

---

## 6. 当前结论与下一步

**当前状态（0.64.60）**：D0 已达 L3（信任底线守住），D1~D4 达 L2，D5 为 L2 未达门禁 L3 → **有条件放行**。

**下一步（严格按门禁缺口 + 未量化排序）**：

| 优先级 | 对应能力 | 动作 | 目标 |
|---|---|---|---|
| **P0** | **D5** | ① uptime 埋点与统计 ② `native/` 吸纳剩余高频确定性流程 ③ 72h 长跑观测 | **D5 → L3（补齐门禁）** |
| **P1** | **D4** | 同步官网 fenshen.plus 版本（当前 0.64.43 → 0.64.60）；量化回滚时效 | D4 一致性回到 100% |
| **P2** | **D2** | 建意图/调度/交付量化评测集；宪法对抗测试集常态化；原生 gate 占比提到 70%+ | D2 有数据可判 |
| **P3** | **D1** | 蒸馏管线 MVP + 盲评题库 | D1 从「占位」变「实测」 |
| **P4** | **D3** | 逐 skill happy path 自测；沉淀组合编排示例 | D3 → L3 |
| **P5** | **D0** | 成本看板 + 预算自动降级 | D0 → L4 |

---

> 本档案随每次发布滚动更新。等级只认实测，不认自述。
