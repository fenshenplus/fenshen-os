# 分身 v3.7 阶段验收 — 签字定稿（deploy v3.7）

> 阶段门禁：v3.7「文件执行器 + 模板依赖 + 手机体验」三个增量（v0.24.0 → v0.25.0）逐项勾选并由使用者（岳衡）签字确认后，deploy v3.7 冻结为发布基线。
> 对应规范：《分身系统-阶段2-部署测试规范与存档逻辑-v1.md》
> 版本链路：deploy v3.6（2026-08-06 定稿）→ v3.7 增量 v0.24.0~v0.25.0 → **deploy v3.7（本次定稿）**
> v3.7 核心：**元神/群聊可读写你的文件；项目模板带依赖关系与元数据；手机端 H5 触控体验完善**

## v3.7 三步增量 Gate（勾选 + 签字）

| # | Phase | 版本 | 能力 | 状态 |
|---|-------|------|------|------|
| 1 | 文件执行器 | v0.24.0 | 新增 `read_file` / `write_file` / `list_files` 三个工具（加入 META_TOOLS，元神与群聊共用）；`file_log` 审计表 + `/api/file/log`；**安全护栏**：路径限用户主目录、拦截 .ssh/.aws/.git//etc 等敏感目录、写入上限 50KB；重活走线程池 | ✅ |
| 2 | 模板进阶（依赖+元数据） | v0.25.0 | 模板模块加 `depends_on` 依赖关系（4 内置模板全部补齐业务依赖，如 支付→登录/注册、购物车→用户中心+商品管理、订单→购物车）；模板加 `meta` 元数据（version/tags/scenario）；旧库迁移 ALTER 补列 + upsert 更新；前端模块编辑行加「依赖模块」多选下拉；套用模板依赖一并带入项目看板（与依赖门禁联动）；存为模板保存依赖与元数据 | ✅ |
| 3 | 手机端 H5 体验打磨 | v0.25.0 | iOS 输入框 16px 防聚焦自动缩放；导航/按钮/角色选择 ≥44px 触控目标；底部导航安全区 `env(safe-area-inset-bottom)`（iPhone 横条适配）；气泡 max-width 88%；弹窗 94vw；房间项加高；tap-highlight 禁用；playwright 390×844 iPhone 视口实测（房间全宽 358px / chat-open 切换 / 返回按钮可见 / 气泡 88% / 弹窗 94vw） | ✅ |

## 验收 Gate（勾选 + 签字）

- [x] 三个增量全部实现并实跑，8002 线上运行版本 v0.25.0
- [x] 每个增量回归测试全过（17/17 多轮，无回退）
- [x] 真实调用实测：
  - 元神「列出桌面元神文件夹」→ list_files 真实列 31 项 ✅
  - 元神「读取 SOUL.md」→ read_file 真实读文件并摘要 ✅
  - 元神「写测试文件」→ write_file 真实落盘 36B，cat 验证内容一致 ✅
  - 安全护栏单测：~/.ssh、/etc/passwd、.git/config、相对路径全部拦截 ✅
  - 模板依赖入库验证：支付/内容模块 depends_on=["登录/注册"] 落库，进依赖门禁 ✅
  - 手机视口（390×844）DOM 实测 6 项全过 ✅
- [x] 无模拟点：工具执行全部真实 + 审计落库（file_log）
- [x] 发行包随增量同步升级：64KB→**66KB**，S4 官网下载 200 验证通过（66721B）
- [x] 端口纪律守住：全程 8002，未触碰 8000（choice-power 生产占用）
- [x] 安全护栏：文件操作限主目录+敏感目录拦截+写入 50KB 上限+全量审计；危险命令拦截沿用；模型 Key 不落前端明文

## 签字记录

| 关键项 | 签字人 | 日期 | 备注 |
|--------|--------|------|------|
| 文件执行器 | 元神 | 2026-08-06 | v0.24.0，读/写/列目录+护栏+审计 |
| 模板进阶（依赖+元数据） | 元神 | 2026-08-06 | v0.25.0，depends_on+meta+前端多选 |
| 手机端 H5 打磨 | 元神 | 2026-08-06 | v0.25.0，触控/安全区/防缩放 |
| 回归 17/17 | 测试 agent | 2026-08-06 | 自动化回归脚本 |
| **最终放行** | **岳衡** | 2026-08-07 | ✅ 已签字，冻结 deploy v3.7 发布基线 |

---

_定稿版本：deploy v3.7（✅ 岳衡 2026-08-07 认可签字，已定稿冻结为发布基线）_

_签字方式：回复「我签」即授权元神代填放行，或亲手在上方「最终放行」行署名与日期。_
