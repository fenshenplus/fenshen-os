# 分身 v3 阶段验收 — 签字定稿（deploy v3.0）

> 阶段门禁：v3 四个增量（v0.9.0 → v0.12.0）逐项勾选并由使用者（岳衡）签字确认后，deploy v3.0 冻结为发布基线。
> 对应规范：《分身系统-阶段2-部署测试规范与存档逻辑-v1.md》+《分身看板-两轴模块化设计-v0.2.md》
> 版本链路：deploy v2.0（2026-07-31 定稿）→ v3 增量 v0.9.0~v0.12.0 → **deploy v3.0（本次定稿）**
> v3 核心：**群聊最前端 + 两层看板（模块×阶段）+ 三层模型（对话/话题/任务）+ 上下文按模块隔离**

## v3 四阶段 Gate（勾选 + 签字）

| # | Phase | 版本 | 能力 | 状态 |
|---|-------|------|------|------|
| A | 模块解构 + 依赖门禁 | v0.9.0 | `modules` 表（name/desc/depends_on/owner_role/status/sort/context_summary）；CRUD + move 依赖检查（进入进行中前依赖须完成，被依赖模块禁删）；成立项目即解构（modules[] 引导）；前端看板弹窗 + 新建项目第3步模块解构 | ✅ |
| B | 话题机制（三层模型落地） | v0.10.0 | `topics` 表（绑定模块的讨论组）+ `tasks` 表（看板卡片，来源话题）；messages 加 topic_id 隔离话题/群聊；话题 CRUD + 提炼任务（`POST /api/topics/{tid}/tasks` 自动进待办）；前端「模块行×任务卡片列」看板 + 话题对话组（点模块名进入）+ 任务可回溯来源话题 | ✅ |
| C | 上下文按模块隔离 | v0.11.0 | `POST /api/topics/{tid}/chat` 模块级上下文：只注入该模块（信息/依赖/context_summary）+ 模块相关任务 + 话题内最近 12 条消息，不污染其它模块/群聊；角色映射（后端/前端/产品/测试→backend/frontend/architect/tester）走对应模型配置；token 针对性投入 | ✅ |
| D | 迭代闭环（任务完成自动沉淀） | v0.12.0 | 任务移到 done 自动触发：①成功经验入库（experiences, source=task）②模块 context_summary 摘要沉淀（上下文释放前留档）③话题内系统通知闭环可见；前端提示沉淀结果 | ✅ |

## 验收 Gate（勾选 + 签字）

- [x] 四个增量全部实现并实跑，8002 线上运行版本 v0.12.0
- [x] 每个增量回归测试全过（17/17 × 4 轮，无回退）
- [x] 前端 H5 全部入口可用：模块看板（模块行×任务列 + 依赖门禁 + 话题对话组 + 提炼任务 + 任务完成沉淀）/ 项目群聊 / 话题对话组（模块级上下文）/ 原有 v2 全部功能
- [x] 发行包已随每个增量同步升级：45KB → 48KB → 49KB → **50KB**，S4 官网下载 200 验证通过（49689B）
- [x] 端口纪律守住：全程 8002，未触碰 8000（choice-power 生产占用）
- [x] 安全护栏：依赖门禁防乱序开发；话题消息隔离防上下文污染；危险命令确认+审计（v1 沿用）；模型 Key 不落前端明文

## 签字记录

| 关键项 | 签字人 | 日期 | 备注 |
|--------|--------|------|------|
| A 模块解构+依赖门禁 | 元神 | 2026-08-05 | v0.9.0，成立项目即解构 |
| B 话题机制（三层模型） | 元神 | 2026-08-05 | v0.10.0，话题绑定模块+提炼任务 |
| C 上下文按模块隔离 | 元神 | 2026-08-05 | v0.11.0，token 针对性投入 |
| D 迭代闭环 | 元神 | 2026-08-05 | v0.12.0，完成自动沉淀 |
| 回归 17/17 ×4 | 测试 agent | 2026-08-05 | 自动化回归脚本 |
| 原型定稿 | 岳衡 | 2026-08-05 | 交互原型 v0.2（群聊+看板+话题+提炼） |
| **最终放行** | **岳衡** | 2026-08-05 | ✅ 已签字，冻结 deploy v3.0 发布基线 |

---

_定稿版本：deploy v3.0（✅ 岳衡 2026-08-05 认可签字，已定稿冻结为发布基线）_

_签字方式：回复「我签」即授权元神代填放行，或亲手在上方「最终放行」行署名与日期。_
